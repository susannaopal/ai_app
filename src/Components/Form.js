import '../CSS/Form.css';

const Form = () => {
  return (
    <form>
      <p>Enter prompt</p>
      <textarea></textarea>
      <button className='submit-btn'>Submit</button>
    </form>
  )
}

export default Form;